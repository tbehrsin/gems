
import THREE from 'three';

export default class NumberTicker extends THREE.Object3D {

  constructor() {

  }

  set value(value) {
    this._value = value;
  }

  update(delta) {

  }

  render(renderer, scene, camera) {

  }
}
